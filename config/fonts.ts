// Mocked for offline build support
export const fontSans = {
  className: "font-sans",
  variable: "--font-sans",
  style: { fontFamily: "Inter, system-ui, sans-serif" },
};

export const fontMono = {
  className: "font-mono",
  variable: "--font-mono",
  style: { fontFamily: "Fira Code, monospace" },
};
